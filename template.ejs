<!-- ***  ***
   ***  后端ejs模板，提供给html-webpack-plugin中间件使用，为server/views 生成相应的后端ejs文件，避免每增加一个路由,就要手动添加页面。
   ***  同时可以自动生成文件名，方便修改文件名
 ***-->

<!DOCTYPE html>
<html>
<head>
    <title><%= htmlWebpackPlugin.options.title %></title>    
     
     <% for (var css in htmlWebpackPlugin.files.css) { %>
       <link href = "<%= htmlWebpackPlugin.files.css[css].replace( new RegExp("[../]+client/release"),'') %>" rel="stylesheet">
     <% } %>

</head>
<body>
    <div id="app"></div>
</body>
    <script type="text/javascript" src="/assets/js/react.js"></script>
    <script type="text/javascript" src="/assets/js/react-dom.js"></script>

    <% for (var chunk in htmlWebpackPlugin.files.chunks) { %>
        <script type="text/javascript"  src= "<%=  htmlWebpackPlugin.files.chunks[chunk].entry.replace( new RegExp("[../]+client/release"),'')  %>"></script>
    <% } %>

</html>